// config.example.js — 이 파일을 복사해 config.js 로 저장한 뒤 키를 채우세요.
//   cp config.example.js config.js
// config.js 는 .gitignore 에 등록되어 저장소에 커밋되지 않습니다.
// 정적 호스팅(GitHub Pages)에서는 배포된 config.js 의 키가 브라우저에 노출되므로,
// 반드시 키에 도메인/사용량 제한(Google AI Studio)을 설정하세요.
window.__CONFIG = {
  GEMINI_API_KEY: "여기에-Gemini-API-키를-입력"
};
